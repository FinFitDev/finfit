part of 'claims_controller.dart';

extension ClaimsControllerSelectors on ClaimsController {
  // Stream<CombinedOffersData> get combinedOffersStream {
  //   return Rx.combineLatest4(allOffersStream, allClaimsStream,
  //       lazyLoadOffsetStream, searchQueryStream.distinct(), getOffersForSearch);
  // }
}
